services:
  mqtt:
    image: eclipse-mosquitto:2
    container_name: iobt-minimal-mqtt
    ports:
      - "1883:1883"
    volumes:
      - ./server/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    restart: unless-stopped
